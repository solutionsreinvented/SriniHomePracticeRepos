<Window x:Class="ProdActivity.UI.Dialogs.AddUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:enums="clr-namespace:ProdActivity.Domain.Enums;assembly=ProdActivity.Domain"
             xmlns:shared="http://schemas.reinvented.com/shared"
             mc:Ignorable="d" 
             Width="350" Height="400"
             WindowStartupLocation="CenterOwner"
             WindowStyle="None"
             ResizeMode="NoResize"
             Background="{DynamicResource WindowBackgroundBrush}">
    
    <Window.Resources>
        <ObjectDataProvider x:Key="UserRoles" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:UserRole" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Add New User" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Employee ID (Username)" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding NewUser.EmployeeId, UpdateSourceTrigger=PropertyChanged}" Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="Full Name" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding NewUser.FullName, UpdateSourceTrigger=PropertyChanged}" Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="Password" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding NewUser.Password, UpdateSourceTrigger=PropertyChanged}" Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="Role" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,5"/>
            <ComboBox ItemsSource="{Binding Source={StaticResource UserRoles}}" SelectedItem="{Binding NewUser.UserRole}" Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Visibility="{Binding HasError, Converter={shared:BoolToVisibilityConverter}}" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Command="{Binding DiscardCommand}" Width="80" Height="30" Margin="0,0,10,0" Background="Transparent" Foreground="{DynamicResource SecondaryTextBrush}" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}"/>
            <Button Content="Save User" Command="{Binding SaveCommand}" Width="100" Height="30" Background="{DynamicResource AccentBrush}" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
