<UserControl
    x:Class="ReIn.Navigation.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ReIn.Navigation.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Width="200" VerticalAlignment="Center">
        <StackPanel>
            <TextBlock
                Margin="0,0,0,25"
                FontSize="28"
                Text="Login" />
            <TextBlock Text="Username" />
            <TextBox Padding="5,2" Text="{Binding Username}" />
            <TextBlock Text="Password" />
            <TextBox Padding="5,2" Text="{Binding Password}" />
            <Button
                Margin="0,10"
                Padding="5,2"
                HorizontalAlignment="Left"
                Command="{Binding LoginCommand}"
                Content="Login" />
        </StackPanel>
    </Grid>
</UserControl>
