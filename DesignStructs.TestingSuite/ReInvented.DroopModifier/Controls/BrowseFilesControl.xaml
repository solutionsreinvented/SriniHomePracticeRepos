<UserControl
    x:Class="ReInvented.DroopModifier.Controls.BrowseFilesControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Root"
    d:DesignHeight="100"
    d:DesignWidth="1300"
    mc:Ignorable="d">

    <Grid Margin="5,5,0,5">

        <!--#region Grid Definitions-->

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="5*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--#endregion-->

        <!--#region Content-->

        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            VerticalAlignment="Center"
            Text="Target Staad Model:" />

        <Grid
            Grid.Row="0"
            Grid.Column="1"
            Height="24">

            <TextBox
                Padding="2,0,0,0"
                HorizontalAlignment="Stretch"
                VerticalContentAlignment="Center"
                IsEnabled="False"
                Text="{Binding StaadModelPath}"
                ToolTip="Browse Staad Model"
                ToolTipService.ShowOnDisabled="True" />
            <Button
                Width="24"
                Margin="1"
                HorizontalAlignment="Right"
                Command="{Binding BrowseStaadModelCommand}"
                Content="..."
                ToolTip="Browse" />

        </Grid>

        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            Margin="0,5"
            VerticalAlignment="Center"
            Text="Source Excel Data File:" />

        <Grid
            Grid.Row="1"
            Grid.Column="1"
            Height="24"
            Margin="0,5">

            <TextBox
                Padding="2,0,0,0"
                HorizontalAlignment="Stretch"
                VerticalContentAlignment="Center"
                IsEnabled="False"
                Text="{Binding ExcelFilePath}"
                ToolTip="Browse Excel File"
                ToolTipService.ShowOnDisabled="True" />
            <Button
                Width="24"
                Margin="1"
                HorizontalAlignment="Right"
                Command="{Binding BrowseExcelFileCommand}"
                Content="..."
                ToolTip="Browse" />

        </Grid>

        <!--#endregion-->

    </Grid>

</UserControl>
