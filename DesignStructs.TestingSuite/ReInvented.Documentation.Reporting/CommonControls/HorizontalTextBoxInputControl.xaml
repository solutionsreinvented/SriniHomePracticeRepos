<UserControl
    x:Class="ReInvented.Documentation.Reporting.Controls.HorizontalTextBoxInputControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ReInvented.Documentation.Reporting.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Root"
    d:DesignHeight="50"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid DataContext="{Binding ElementName=Root}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding LabelColumnWidth}" />
            <ColumnDefinition Width="{Binding TextBoxColumnWidth}" />
        </Grid.ColumnDefinitions>
        <TextBlock VerticalAlignment="Center" Text="{Binding Label}" />
        <TextBox
            Grid.Column="1"
            Height="24"
            Margin="5,0"
            Padding="5,2"
            VerticalAlignment="Center"
            VerticalContentAlignment="Center"
            Text="{Binding Value}" />
    </Grid>
</UserControl>
