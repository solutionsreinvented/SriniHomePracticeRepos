<Window
    x:Class="ReInvented.Animations.Views.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ap="clr-namespace:ReInvented.Animations.AttachedProperties"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ReInvented.Animations.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Home"
    Width="1000"
    Height="600"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border
            Grid.Row="0"
            Background="Gainsboro"
            BorderBrush="BurlyWood"
            BorderThickness="0.8"
            CornerRadius="5">
            <ContentControl Content="{Binding CurrentViewModel}" />
        </Border>
        <UniformGrid
            Grid.Row="1"
            Height="32"
            Margin="0,10"
            Columns="2">
            <Button
                Margin="0,0,10,0"
                Command="{Binding RecentItemsCommand}"
                Content="Recent Items" />
            <Button
                Margin="10,0,0,0"
                Command="{Binding NewProjectCommand}"
                Content="New Project" />
        </UniformGrid>
    </Grid>
</Window>
