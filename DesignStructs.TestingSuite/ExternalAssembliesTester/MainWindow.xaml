<Window
    x:Class="ExternalAssembliesTester.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ap="clr-namespace:ExternalAssembliesTester.AttachedProperties"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ExternalAssembliesTester.ViewModels"
    Title="MainWindow"
    Width="800"
    Height="200"
    mc:Ignorable="d">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ListBox
            Grid.Row="0"
            ap:ListBoxProperties.AutoScroll="True"
            ItemsSource="{Binding Items}" />
        <Button
            Grid.Row="1"
            Height="24"
            Command="{Binding AddItemCommand}"
            Content="Add New Item" />
    </Grid>
</Window>
